---
name: lead-analysis
description: 'Ежедневный анализ лидов клиники доктора Ласкова в Bitrix24. Запускать при словах "анализ лидов", "прогон", "запусти анализ", "отчёт по лидам", или по расписанию (утро). День отчёта = вчера (МСК). Группы: A (закрытые в день отчёта), B (дата следующего контакта = день отчёта), C (нет даты, активность в день отчёта). Анализирует коммуникацию батчами по 10, заполняет CRM, отчёт в chat31598: покрытие → автоматизация → к действию → UF-gap компактно.'
---

# Анализ лидов Б24 — клиника доктора Ласкова

## Инфраструктура

- **Вебхук**: `BITRIX24_WEBHOOK_URL` из env (входящий REST prod `laskov-partners`; scope: crm, telephony, im, imopenlines, imbot, disk, tasks — не коммитить URL в git). Тексты чатов в фазе 1 ожидаются, когда у webhook DWH есть `im`+`imopenlines` (`openlines_scope=true` в manifest).
- **Отчёт → чат**: `chat31598` (только 🔴 и ⚠️ лиды)
- **CSV-лог**: Б24 Disk, папка "Аналитика КЦ", файл `leads_log.csv` (дописывать ежедневно, все лиды)
- **Обратная связь (несогласия)**: кнопки бота «Оценка AI» (imbot 23032) в chat31598 — «❌ Не согласен» под лидом. Обработку/гейт/дообучение ведёт бот + scheduled `lead-feedback-apply` (см. Шаг 8). Текстовый [FEEDBACK] и Google-форма выведены.

## ⚠️ КОДИРОВКА PowerShell — ОБЯЗАТЕЛЬНО

**ВСЕГДА** использовать `UTF8.GetBytes` для ЛЮБОГО вызова Bitrix24 API через PowerShell.
Без этого кириллица и эмодзи превращаются в `?` — и в IM-чате, и в полях лидов.

```powershell
function B24-Post($method, $payload) {
    $bytes = [System.Text.Encoding]::UTF8.GetBytes((ConvertTo-Json $payload -Depth 5))
    return (Invoke-RestMethod -Uri ($WEBHOOK+$method) -Method POST -Body $bytes -ContentType "application/json; charset=utf-8").result
}
```

## АРХИТЕКТУРА ВЫПОЛНЕНИЯ

```
Фаза 1 — Python на DWH (не Claude Desktop)
  /opt/clinic-dwh/lead_analysis_fetch.py  (репо hemonc-team/dwh-etl)
  День отчёта = вчера (Europe/Moscow): --report-date=yesterday (default / cron)
  Собирает лиды A/B/C за этот день + звонки + чаты (если openlines_scope) + таймлайн + UF
  Батчи: /opt/clinic-dwh/logs/lead_analysis/latest/
         catalogs.json, manifest.json (report_date, openlines_scope, uf_gap), batch_*.json
  Cron: 09:10 МСК (06:10 UTC), после gpt_transcripts 09:00 МСК
  В лог/Telegram — только ID и счётчики, без ФИО и текстов

Фаза 2 — суждение (НЕ Claude на DWH в РФ — стоп 25.08)
  Вход: батчи latest/ с DWH (scp/rsync или Cursor); дата в шапке = manifest.report_date
  Рантайм: Cursor вручную / Claude Code на GCP-VM / иной LLM — см. phase2-plan §3
  Для каждого лида: шаги 4A → 4B → 4C → 4D → 4E → 4F → 5
  dry-run: только файлы отчёта; live: CRM + chat31598
  НЕ вызывает crm.lead.list / voximplant / imopenlines / Chrome
  НЕ mass launchCopilot / browser-дорасшифровка (политика #24604)
```

Если каталога `latest/` нет — сначала на DWH:
`cd /opt/clinic-dwh && ./venv/bin/python lead_analysis_fetch.py --report-date=yesterday`

## ⛔ STOP-GATE #0 — ПОДСЧЁТ ЛИДОВ ПЕРЕД АНАЛИЗОМ

**День отчёта = вчера (календарный день, Europe/Moscow).** Утренний прогон
смотрит **предыдущий** день, не «сегодня». В фазе 1: `manifest.report_date`
(или `lead_analysis_fetch.py --report-date=yesterday`).

**КРИТИЧЕСКАЯ ПРОВЕРКА:** Прежде чем начинать анализ, убедиться что:

```
REPORT_DATE = вчера МСК (из manifest / --report-date)

✅ Group A: ПОЛУЧИТЬ ACTUAL COUNT (не предполагать 10 или любое число)
   Фильтр: DATE_CLOSED в [REPORT_DATE 00:00 … REPORT_DATE+1 00:00)
           И STATUS_ID в (CONVERTED, JUNK, "4", "7", "20")
   Результат: [N] лидов
   
✅ Group B: ПОЛУЧИТЬ ACTUAL COUNT
   Фильтр: UF_CRM_1599826765930 в том же календарном дне REPORT_DATE
   Результат: [N] лидов
   
✅ Group C: ПОЛУЧИТЬ ACTUAL COUNT
   Фильтр: UF_CRM_1599826765930 пусто И LAST_ACTIVITY_TIME в дне REPORT_DATE
   Результат: [N] лидов

НИКОГДА не предполагать количество. НИКОГДА не анализировать "первые 10" если их больше.
ВСЕГДА разбить на батчи по 10 и анализировать ВСЕ батчи подряд.
```

---

## ОБЯЗАТЕЛЬНЫЙ ЧЕКЛИСТ ПЕРЕД ЗАПУСКОМ

Перед стартом убедись, что выполнишь ВСЕ шаги. Это не рекомендации — это контракт:

```
[ ] STOP-GATE #0: Подсчитаны лиды всех групп (не предполагаются количества)
[ ] Шаг 1: Загружены свежие справочники (статусы, enum-значения, сотрудники)
[ ] Шаг 2: Взяты лиды по фильтру A/B/C, разбиты на батчи по 10
[ ] Шаг 3: Для каждого лида собраны звонки + чаты (все коннекторы)
[ ] Шаг 3-EXIST: если voximplant/imopenlines вернули пусто — ОБЯЗАТЕЛЬНО проверено crm.activity.list ПЕРЕД выводом "коммуникации нет" (живой инцидент 21.07: #89342/#87944/#87800)
[ ] Шаг 4A: Проверена полнота расшифровок (R01)
[ ] Шаг 4B: Дан ответ на 4 вопроса "Реальная ситуация"
[ ] Шаг 4C: Проверена достаточность попыток связи (R02)
[ ] Шаг 4D: Записаны поля CRM через API (Вариация + Препарат + Статус + Задача; ТегиКЦ — только если пусто и тег очевиден из разговора, иначе не писать / «Другие» без угадывания)
[ ] Шаг 4E: Проверены ссылки 1С vs стадия лида (R06)
[ ] Шаг 4F: Верифицированы ФИО врачей (R05)
[ ] Шаг 5: Каждый лид классифицирован (🔴/⚠️ actionable / ⚠️ UF-gap / ✅/ℹ️)
[ ] Шаг 6: Отчёт: покрытие → автоматизация → к действию → UF-gap компактно; кнопки бота 23032 только под «К действию»
[ ] Шаг 7: Все лиды дописаны в CSV-лог
[ ] Шаг 8: обратную связь ведёт бот 23032 (кнопки) + scheduled apply — lead-analysis текст не обрабатывает
```

Если по какой-то причине шаг пропускается — явно написать почему.

---

## Шаг 1 — Подготовка справочников

Загрузить через REST API (НИКОГДА не хардкодить):

```python
import os
WEBHOOK = os.environ["BITRIX24_WEBHOOK_URL"]  # trailing slash; из vault / .env, не из SKILL

# 1. Маппинг статусов лида
statuses = POST(WEBHOOK + "crm.status.list.json", {"filter": {"ENTITY_ID": "STATUS"}})
# → {STATUS_ID: "название стадии"}

# 2. Enum-значения кастомных полей
userfields = POST(WEBHOOK + "crm.lead.userfield.list.json", {})
# Извлечь: UF_CRM_1706806189 (ТегиКЦ), UF_CRM_1768816592560 (Вариация приема),
#           UF_CRM_1748956354216 (Препарат), UF_CRM_1747754997445 (Статус детальный)
# → {field_id: {enum_id: "название"}}

# 3. Справочник сотрудников (для верификации врачей, R05)
users = POST(WEBHOOK + "user.get.json", {"ACTIVE": True})
# → {user_id: "Фамилия Имя Отчество"}
```

---

## Шаг 2 — Выборка лидов

**Канон фазы 1:** не дублировать выборку вручную — брать батчи `latest/` с DWH.
Ниже — логика фильтров (должна совпадать с `lead_analysis_fetch.py`).

**День отчёта `REPORT_DATE` = вчера (МСК)** при утреннем прогоне.

Брать лид если выполняется ХОТЯ БЫ ОДНО из условий:

```
A) Закрыт в день отчёта
   DATE_CLOSED ∈ [REPORT_DATE 00:00 … REPORT_DATE+1 00:00)
   И STATUS_ID в финальных (CONVERTED, JUNK, "4", "7", "20")
   → анализируем итог: правильно ли закрыт
   Не DATE_MODIFY: gpt_transcripts сдвигает DATE_MODIFY у старых закрытых.

B) Открытый + дата следующего контакта = день отчёта
   UF_CRM_1599826765930 ∈ [REPORT_DATE 00:00 … REPORT_DATE+1 00:00)
   → должны были связаться в день отчёта

C) Открытый + нет даты следующего контакта
   + LAST_ACTIVITY_TIME ∈ [REPORT_DATE 00:00 … REPORT_DATE+1 00:00)
   → нет плана, активность зависла в день отчёта
   (пустую дату следующего контакта фильтровать в Python, не REST `=UF:""`)
```

```python
from datetime import datetime, timedelta, date
from zoneinfo import ZoneInfo
MSK = ZoneInfo("Europe/Moscow")
# утро: report_date = вчера МСК
report_date = datetime.now(MSK).date() - timedelta(days=1)
start = f"{report_date.isoformat()}T00:00:00"
end = f"{(report_date + timedelta(days=1)).isoformat()}T00:00:00"

CLOSED_STATUSES = ["CONVERTED", "JUNK", "4", "7", "20"]
# SELECT_FIELDS — как в lead_analysis_fetch.py

leads_A = list_leads({">=DATE_CLOSED": start, "<DATE_CLOSED": end, "STATUS_ID": CLOSED_STATUSES})
leads_B = list_leads({">=UF_CRM_1599826765930": start, "<UF_CRM_1599826765930": end, "!STATUS_ID": CLOSED_STATUSES})
c_raw = list_leads({">=LAST_ACTIVITY_TIME": start, "<LAST_ACTIVITY_TIME": end, "!STATUS_ID": CLOSED_STATUSES})
# leads_C = filter empty UF_CRM_1599826765930 in Python
```

Пагинация: ID-курсор (`start: -1`). Логировать counts A/B/C + `report_date`.

---

## Шаг 3 — Сбор коммуникации для каждого лида

Для каждого лида собрать ВСЕ точки контакта:

### Звонки
```python
calls = POST(WEBHOOK + "voximplant.statistic.get.json", {
    "FILTER": {"CRM_ENTITY_TYPE": "LEAD", "CRM_ENTITY_ID": lead_id},
    "ORDER": {"CALL_START_DATE": "ASC"}
})
# Сохранить: дата, тип (1=входящий, 2=исходящий), длительность, TRANSCRIPT_ID
```

> ✅ **ПЕРВЫЙ ПРИОРИТЕТ — поле `UF_CRM_GPT_TRANSCRIPT` лида.**
> Если поле уже заполнено (утренний cron на DWH `gpt_transcripts.py`, 09:00 МСК) —
> **использовать его напрямую, браузер не открывать, getCopilotTranscript не вызывать.**
> Если поле пустое — не открывать Chrome: пометить текст недоступным и идти дальше
> (Шаг 3-EXIST). Scheduled-скилл `bitrix-gpt-transcripts` в Claude Desktop снят.

> ⚠️ **ПРОБЛЕМА: voximplant.statistic.get и imopenlines.crm.chat.get не всегда возвращают ВСЮ коммуникацию по лиду.**
> Bitrix24 иногда физически привязывает звонок/чат к Contact, а не к Lead (типично для уже
> знакомых пациентов, `IS_RETURN_CUSTOMER=Y`) — тогда фильтр по LEAD отдаёт пустой результат,
> хотя звонок/чат реально виден в таймлайне лида (`crm.activity.list`).
> **Живой инцидент 21.07.2026**: лиды #89342, #87944, #87800 были ошибочно помечены в отчёте
> как "коммуникация не зафиксирована" — voximplant.statistic.get по LEAD вернул `total:0`,
> хотя реальные звонки (в т.ч. успешный 112-секундный разговор с уже готовым GPT-саммари
> в поле COMMENTS лида) были видны в `crm.activity.list`. Раздел ниже — обязательный фикс
> этого класса ошибки.

#### 3-EXIST. ОБЯЗАТЕЛЬНАЯ проверка ФАКТА коммуникации (чистый REST, БЕЗ БРАУЗЕРА)

⛔ **Этот под-шаг НЕ пропускать НИ ПРИ КАКИХ обстоятельствах, включая недоступность Chrome MCP.**
Он узнаёт только ФАКТ звонка/чата (дата, длительность, направление) — обычным REST-вызовом
вебхука, никакой браузер/SESSID для этого не нужен. Браузер нужен ТОЛЬКО в Шаге 3Б/3В, чтобы
получить ТЕКСТ расшифровки — это отдельная, необязательная задача (см. ниже).

**Когда запускать (любое из условий — достаточно одного):**
- `voximplant.statistic.get` вернул **полностью пустой список** (`total: 0`) — не только
  "TRANSCRIPT_ID=null у части звонков", а вообще ни одной записи;
- у всех найденных звонков `TRANSCRIPT_ID = null`;
- `imopenlines.crm.chat.get` вернул пустой результат (чатов не найдено).

**Проверка звонков (Python/REST, без браузера):**
```python
calls_timeline = POST(WEBHOOK + "crm.activity.list.json", {
    "filter": {"OWNER_TYPE_ID": 1, "OWNER_ID": lead_id, "TYPE_ID": 2},  # TYPE_ID=2 — звонок
    "select": ["ID", "SUBJECT", "START_TIME", "END_TIME", "DESCRIPTION", "SETTINGS"]
})
# Если result непустой — звонок(и) РЕАЛЬНО был(и), даже если voximplant.statistic.get по LEAD
# вернул total:0. Длительность = END_TIME - START_TIME. SETTINGS.MISSED_CALL=true → пропущенный.
```

**Проверка чатов (Python/REST, без браузера):**
```python
chats_timeline = POST(WEBHOOK + "crm.activity.list.json", {
    "filter": {"OWNER_TYPE_ID": 1, "OWNER_ID": lead_id, "PROVIDER_ID": "IMOPENLINES_SESSION"},
    "select": ["ID", "SUBJECT", "START_TIME", "ASSOCIATED_ENTITY_ID"]
})
# Если result непустой — чат-сессия РЕАЛЬНО была, даже если imopenlines.crm.chat.get вернул пусто.
```

**⛔ ЗАПРЕЩЕНО писать в отчёте «коммуникации не было» / «calls пуст» / «нет ни одного
зафиксированного контакта», пока 3-EXIST не выполнен и подтверждённо не вернул пусто по ОБОИМ
проверкам (звонки И чаты).** Если 3-EXIST нашёл активность — это меняет Шаг 4B и Шаг 5: у лида
ЕСТЬ факт контакта, даже если текста расшифровки нет (см. Шаг 3Б/3В ниже — опционально).

#### Если TRANSCRIPT_ID = null У ВСЕХ ЗВОНКОВ, или voximplant.transcription.get возвращает 0 реплик — опциональная дорасшифровка через браузер

> ⚠️ **ВАЖНО**: `voximplant.transcription.get` с 0 репликами ≠ "пустая расшифровка". Это значит BitrixGPT ещё не обработал. В обоих случаях (TRANSCRIPT_ID=null И 0 реплик) — использовать браузерный `getCopilotTranscript`.

Этот блок — ТОЛЬКО про получение ТЕКСТА расшифровки для уже найденных на 3-EXIST активностей.
Требует Chrome MCP/SESSID. Если браузер недоступен — это НЕ повод писать «коммуникации не
было»: пишем «звонок [дата, длительность из 3-EXIST] зафиксирован в таймлайне, текст
расшифровки недоступен (браузер недоступен)» и продолжаем анализ без блокировки прогона.

**Шаг 3Б. Для каждой активности (уже найденной на 3-EXIST) попробовать получить расшифровку BitrixGPT:**

> ⚠️ **КРИТИЧНО — НИКОГДА не использовать синхронный XHR** (`xhr.open(..., false)`).
> Синхронный XHR блокирует поток Chrome — таб виснет после 2-3 вызовов.
> **ВСЕГДА** использовать async fetch + window-хранилище:

```javascript
// ШАГ 1 — запустить fetch (не блокирует поток), вернуть 'started' немедленно
window.__transcripts = {};
const sessid = BX.bitrix_sessid();  // BX доступен напрямую, без iframe

fetch('/bitrix/services/main/ajax.php?action=crm.timeline.ai.getCopilotTranscript', {
  method: 'POST',
  headers: {'Content-Type': 'application/x-www-form-urlencoded'},
  body: `activityId=${actId}&ownerTypeId=1&ownerId=${lead_id}&sessid=${sessid}`
}).then(r=>r.json()).then(d=>{
  window.__transcripts[actId] = d?.data?.aiJobResult?.transcription || null;
});
'started'  // CDP видит это немедленно, таб не виснет

// ШАГ 2 — подождать 4-5 секунд (через PowerShell Sleep), затем прочитать:
window.__transcripts[actId]
// Расшифровка: строка текста или null (BitrixGPT не обработал)
```

**Шаг 3В. `launchCopilot` / browser-дорасшифровка — политика #24604**

⛔ **Массово запрещено** в ежедневном прогоне фазы 2 и в автоматизациях:
не гонять mass `crm.timeline.ai.launchCopilot` / Chrome CDP «дорасшифровку»
по всем `has_tid` / REST `getTranscript=null` / UF-gap лидам.

✅ **Точечно можно** — один-два кейса по явной просьбе человека или разбору
инцидента (не как шаг отчёта).

Причина: REST только **читает** AI-store; массовый launchCopilot не закрывает
дыру UF-gap и раздувает нагрузку. Утренний `gpt_transcripts.py` — канон доставки
текста в UF. Пустой UF при длинном звонке → ⚠️ UF-gap компактно, не silent fail.

Устаревший пример точечного launch (не для daily):
```javascript
// ТОЛЬКО точечно, не в цикле по батчу
fetch('/bitrix/services/main/ajax.php?action=crm.timeline.ai.launchCopilot', {
  method: 'POST',
  headers: {'Content-Type': 'application/x-www-form-urlencoded'},
  body: `activityId=${actId}&ownerTypeId=1&ownerId=${lead_id}&sessid=${BX.bitrix_sessid()}`
});
```

**Обработка нескольких активностей одним вызовом:**
```javascript
// Можно forEach — все fetch'и запускаются параллельно, поток не блокируется
window.__transcripts = {};
const sessid = BX.bitrix_sessid();
[actId1, actId2, actId3].forEach(actId => {
  fetch('/bitrix/services/main/ajax.php?action=crm.timeline.ai.getCopilotTranscript', {
    method: 'POST',
    headers: {'Content-Type': 'application/x-www-form-urlencoded'},
    body: `activityId=${actId}&ownerTypeId=1&ownerId=${lead_id}&sessid=${sessid}`
  }).then(r=>r.json()).then(d=>{
    window.__transcripts[actId] = d?.data?.aiJobResult?.transcription || null;
  });
});
'started'
// Подождать 5с, прочитать: JSON.stringify(window.__transcripts)
```

> Полный алгоритм: `bitrix-gpt-transcripts/SKILL.md` (scheduled task).

### Чаты (WhatsApp, Max, Telegram)

Фаза 1 уже кладёт чаты в JSON, если `openlines_scope=true` (webhook DWH с
`im` + `imopenlines`). Если `openlines_scope=false` — текстов сообщений нет,
остаётся факт сессии из `timeline` (3-EXIST). Не вызывать imopenlines из фазы 2.

```python
chats = POST(WEBHOOK + "imopenlines.crm.chat.get.json", {
    "CRM_ENTITY_TYPE": "lead",
    "CRM_ENTITY": lead_id,
    "ACTIVE_ONLY": "N"
})
for chat in chats:
    if chat["CHAT_ID"]:
        messages = POST(WEBHOOK + "im.dialog.messages.get.json", {
            "DIALOG_ID": f"chat{chat['CHAT_ID']}",
            "LIMIT": 200
        })
        # Фильтр: author_id != 0, text не пустой, нет 'bx-messenger-content-item-ol'
```

---

## Шаг 4 — Анализ каждого лида

### 4A. Полнота расшифровки (R01 / UF-gap) — канон с 2026-08-25 (отчёт с 2026-08-27)

**Источник правды — поле `UF_CRM_GPT_TRANSCRIPT` (лид, либо fallback с контакта в JSON фазы 1), не `TRANSCRIPT_ID` телефонии.**  
Ответ ТП Bitrix 25.08: `TRANSCRIPT_ID` ≠ AI-диалог CoPilot; `getTranscript` null = в AI-хранилище нет записи.

**UF-gap:** есть состоявшийся звонок ≥15 сек (`CALL_FAILED_CODE=200`), а текст в поле пуст.

- **ДА (uf_gap=true / `uf_gap_detail.report_mark`)** → классификация минимум ⚠️ **UF-gap (данные)**;
  в отчёте — **только компактный блок** «UF-gap» (счётчик + % + список ID), **без**
  карточки Хотел/Ситуация/Исправить и без фразы «Не судить речь» на каждый лид.
  Выводы по речи не делать. По чатам / факту звонка / CRM / 1С — можно.
  Если есть ещё actionable-флаг (R02/R06/F03/риск потери/follow-up) — лид идёт в
  блок «К действию» (+ пометка UF-gap в ситуации). День анализа **не стопать**.
- **НЕТ** → можно разбирать содержание разговора по тексту UF.
- **Короткий / несостоявшийся звонок** → норма, не UF-gap.

Устаревшее правило «звонок позже последнего с TRANSCRIPT_ID» — не использовать.

> Дорасшифровка AI-store — **не** обязанность фазы 2 daily. **Не** mass `launchCopilot`.
> После утреннего `gpt_transcripts` пустой UF при длинном звонке = UF-gap компактно.

### 4B. Реальная ситуация (обязательный текст в отчёт)

> ⛔ **КРИТИЧЕСКОЕ ПРАВИЛО: НИКОГДА НЕ ПРЕДПОЛАГАЙ ДУБЛЬ БЕЗ ЧТЕНИЯ ВСЕХ СООБЩЕНИЙ**
>
> Если у лида похожее имя или телефон на другой лид — это НЕ основание пропускать анализ.
> Ты ОБЯЗАН прочитать все сообщения и звонки текущего лида перед любым выводом.
>
> **Пример-антипаттерн (лид #64274, 19.05.2026):**
> Claude пропустил анализ, предположив "возможный дубль" по совпадению имени.
> В результате пропущен критический факт: пациентка Инна Халилюлина в этот же день
> совершала оплату 684 000 руб. наличными — лид был ошибочно помечен как СПАМ.
> **Это прямой финансовый и репутационный ущерб клинике.**
>
> Правило: сначала читаешь ВСЮ коммуникацию, потом делаешь выводы. Никогда наоборот.

> ⛔ **ПРЕДУСЛОВИЕ ПЕРЕД ВЫВОДОМ "коммуникации нет"**: если voximplant/imopenlines по этому
> лиду вернули пусто — сначала подтвердить, что Шаг 3-EXIST (`crm.activity.list`) РЕАЛЬНО
> выполнен и ТОЖЕ вернул пусто по звонкам и чатам. Если 3-EXIST не выполнялся — это
> блокирующая ошибка сбора данных, а не законный вывод "коммуникации нет" (живой инцидент
> 21.07: #89342 — звонок 112 сек был, отчёт написал "всё пусто", не проверив таймлайн).

На основе ВСЕЙ коммуникации (включая найденное на Шаге 3-EXIST, даже без текста расшифровки) ответить явным текстом на 4 вопроса:
1. **Что хотел пациент?**
2. **Получил ли то, что хотел?**
3. **Записался ли?** → проверить `UF_CRM_1732558654060`: содержит `e1cib/data/Документ.ОказаниеУслуг` → записан (R03)
4. **Отказался явно или просто не отвечает?**

### 4C. Достаточность попыток связи (R02)

Только если: российский номер (+7) И активный запрос (запись или покупка препарата).

Требование: ≥ 3 исходящих касания, из которых:
- ≥ 1 через Telegram или Max
- ≥ 1 исходящий звонок (CALL_TYPE = 2)

Если меньше → флаг `C: ⚠️ Недостаточно попыток связи ([N] из 3)`

### 4D. Заполнение полей CRM

Правила заполнения — в `references/field_rules.md`. Читать обязательно.

#### ТегиКЦ — подстраховка, не угадывание

Перед записью `UF_CRM_1706806189`:

1. Если поле уже заполнено оператором — **не трогать**.
2. Если пусто (`False` / [] / «не выбрано») и из разговора тег **очевиден** — записать соответствующий enum ID.
3. Если **сомнение** — **не писать** (не угадывать). Явно не подходит ни один узкий тег, но запрос есть → можно `Другие` (1894).
4. Источник истины по ТегиКЦ — КЦ; LLM не должен массово размечать сомнительные лиды ради «заполненности».

#### Поле "Препарат" — дефицит справочника

Если в тексте упомянут препарат, но его нет в enum `UF_CRM_1748956354216`:

1. **ОСТАНОВИТЬ запись** для этого лида (не записывать "Не применимо")
2. **Вывести в чат пользователю:**
   ```
   ⚠️ ДЕФИЦИТ СПРАВОЧНИКА: лид #[ID]
   Упомянут препарат "[название из расшифровки]"
   Не найден в enum поля Препарат.

   Подтвердите или исправьте название:
   1. Добавить "[название]" → запишу в лид и справочник
   2. Это [другое название] → укажите правильное
   3. Не применимо → пропустить поле
   ```
3. **Дождаться ответа пользователя** перед записью в CRM
4. После согласования — добавить в `references/field_rules.md` (раздел "Известные искажения") и записать в лид

> Пример: лиды #82754 и #64274 — препарат "Датровей" (датopotamab deruxtecan).
> Отсутствовал в справочнике на 19.05.2026.

После определения значений — записать в CRM:
```python
POST(WEBHOOK + "crm.lead.update.json", {
    "id": lead_id,
    "fields": {
        "UF_CRM_1706806189": [tegikc_id],          # ТегиКЦ — массив (multiple=Y)
        "UF_CRM_1768816592560": variacia_id,        # Вариация приема
        "UF_CRM_1748956354216": drug_id,            # Препарат — массив (multiple=Y)
        "UF_CRM_1747754997445": status_detail_id,   # Статус детальный
        "UF_CRM_1747750972275": task_text           # Задача
    }
})
```

### 4E. Проверка ошибок администратора (R06)

| Проверка | Условие ошибки |
|---|---|
| Ссылка 1С (Запись) | Стадия = "Запись на прием", `UF_CRM_1732558654060` НЕ содержит `e1cib/data/Документ.ОказаниеУслуг` |
| Ссылка 1С (Лист ожидания) | Стадия = "Лист ожидания", `UF_CRM_1761919557442` НЕ содержит `e1cib/data/РегистрСведений.ЛистОжидания` |
| Дата следующего действия | Контакт состоялся, `UF_CRM_1599826765930` не обновлена согласно договорённости из расшифровки |

При обнаружении → флаг `E: ⚠️ Ошибка администратора: [описание]`

### 4F. Верификация ФИО врачей (R05)

Сверить упомянутые фамилии со справочником. Искажения — в `references/doctor_names.md`.

---

## Шаг 5 — Классификация итога

| Классификация | Критерий | В отчёте |
|---|---|---|
| 🔴 Потеря | Пациент хотел записаться/купить, явный уход или брошен без follow-up | Блок «К действию» — полная карточка |
| ⚠️ Возможная потеря / процесс | R02/R06/F03/follow-up/стадия/дата контакта и т.п. | Блок «К действию» — полная карточка |
| ⚠️ UF-gap (данные) | Звонок ≥15с, UF пуст; **нет** других actionable-флагов | Только компактный блок UF-gap (ID), **не** разворачивать |
| ℹ️ Информационное | Пациент получил информацию, запись не планировалась | Только счётчик |
| ✅ Ок | Всё корректно, поля заполнены, стадия верна | Только счётчик |

Если uf_gap **и** actionable — считать ⚠️ к действию (карточка), в UF-gap-списке тоже можно упомянуть ID.

Определить направление из текста коммуникации:
`онкология / гематология / ревматология / психиатрия / другое`

---

## ⚠️ ШАГИ 4A-5 СОДЕРЖАТ АНАЛИЗ, НЕ ПРОСТО КЛАССИФИКАЦИЮ

**ОБЯЗАТЕЛЬНО:** Для каждого лида блока **«К действию»** (🔴 и actionable ⚠️)
собрать при анализе. Чистый UF-gap — **исключение** (карточка не нужна):

- **Хотел:** ЧТО ПАЦИЕНТ ЗАПРАШИВАЛ (из названия лида, первого сообщения, звонка)
  - Пример: "Запись к доктору Ласкову", "Онлайн-консультация"
  
- **Ситуация:** ЧТО ПРОИЗОШЛО (результат коммуникации из звонков/чатов)
  - Пример: "Предложили 3 июня 14:00. Пациент не ответил"
  
- **Флаги:** СПЕЦИАЛЬНЫЕ ПОМЕТКИ (из полей CRM)
  - `C` = FLAG_C (есть/нет)
  - `E` = Лид в "Потерянный" + ссылка 1С (запись была, почему статус неправильный?)
  - `F03` = Статус изменён с "Отказ" на "Пациент не отвечает" (явного отказа не было)
  
- **Исправить:** КОНКРЕТНОЕ ДЕЙСТВИЕ для оператора
  - Пример: "Написать в Telegram — уточнить решение"

**⛔ ЕСЛИ У «К ДЕЙСТВИЮ» НЕТ ЭТИХ 4 ПОЛЕЙ — ОТЧЁТ НЕПОЛНЫЙ. Чистый UF-gap без карточек — норма.**

---

## Шаг 6 — Отправка отчёта в chat31598

Дата в шапке = **день отчёта** (`manifest.report_date` = вчера МСК), не дата прогона.
В теле карточек — только блок **«К действию»** (🔴 + actionable ⚠️).
✅ / ℹ️ / чистый UF-gap — в счётчиках и компактных блоках, без карточек.

### 🔒 САМОПРОВЕРКА — ОБЯЗАТЕЛЬНЫЙ ВЫВОД ПЕРЕД ОТПРАВКОЙ

> ⛔ **СТОП. НИКОГДА не отправляйте отчёт молча!**
> 
> Перед отправкой в chat31598 — ОБЯЗАТЕЛЬНО вывести таблицу ниже в чат с пользователем.
> Таблица должна быть видна в ответе. Если хоть одна строка ✗ — исправить и повторить.
> 
> **ЕСЛИ ВЫ ОТПРАВИЛИ ОТЧЁТ БЕЗ ЭТОЙ ТАБЛИЦЫ — ЭТО ОШИБКА И ТРЕБУЕТ ИСПРАВЛЕНИЯ.**

Вывести дословно и заполнить:

```
САМОПРОВЕРКА ОТЧЁТА:
[ ] Дата = report_date (вчера МСК), не «сегодня прогона» — ДА/НЕТ
[ ] Есть блок «Покрытие данными» — ДА/НЕТ
[ ] Есть блок «Сделано автоматизацией» (хотя бы ТегиКЦ было→стало) — ДА/НЕТ
[ ] Чистые uf_gap НЕ развёрнуты карточками — ДА/НЕТ
[ ] Ссылки [URL=.../crm/lead/details/{ID}/]#{ID}[/URL] у «К действию» — ДА/НЕТ
[ ] Хотел/Ситуация/Флаги/Исправить у каждого «К действию» — ДА/НЕТ
[ ] BB-код, не markdown — ДА/НЕТ
[ ] Счётчик 🔴/⚠️/✅/ℹ️ + разбивка ⚠️ (UF-gap vs к действию) — ДА/НЕТ
[ ] Кнопки бота 23032 только под «К действию» (не под чистый UF-gap) — ДА/НЕТ
→ Все ДА? → отправляю в chat31598
→ Есть НЕТ? → исправляю, повторяю проверку
```

### Формат сообщения

```
📊 Отчёт за [REPORT_DATE]: [total] лидов (A[n] B[n] C[n])
🔴 [N] | ⚠️ [N] (UF-gap: [G], к действию: [A]) | ✅ [N] | ℹ️ [N]
Фаза 1: openlines_scope=[true/false] | uf_gap [G]/[long] ([pct]%)

📎 Покрытие данными
• С текстом (UF/чат): [T]
• UF-gap (звонок ≥15с, UF пуст): [G] ([pct]% от длинных)
• Без коммуникации / только короткий звонок: [Z]

🤖 Сделано автоматизацией
• ТегиКЦ: было [x filled / T с текстом] → стало [y], записано +[w]
• (опц.) Вариация / Препарат / Статус / Задача: кратко N записей

━━━━━━━━━━━━━━━━━━━━━━━
К действию КЦ

🔴 [URL=https://laskov-partners.bitrix24.ru/crm/lead/details/{ID}/]#{ID}[/URL] — [Оператор]
Хотел: …
Ситуация: …
Флаги: …
Исправить: …

⚠️ [URL=…]#{ID}[/URL] — [Оператор]
… (только actionable; НЕ чистый UF-gap)

━━━━━━━━━━━━━━━━━━━━━━━
UF-gap (расшифровка недоступна — по речи не судим)
[G] лидов ([pct]%). ID: #…, #…, …
(если G=0 — секцию не писать)

━━━━━━━━━━━━━━━━━━━━━━━
Не согласны? Кнопки «❌ Не согласен» — только под лидами блока «К действию».
```

Если «К действию» пуст — шапка + покрытие + автоматизация + UF-gap (если есть), без карточек.

### Кнопки обратной связи — постит бот «Оценка AI» (23032)
После текстового отчёта по КАЖДОМУ лиду блока **«К действию»** (не по чистому UF-gap)
отправить ОТДЕЛЬНОЕ сообщение ОТ БОТА с кнопкой «❌ Не согласен» (бот 23032):

> ⚠️ ПОСТИТЬ ЭТО ТОЛЬКО ЧЕРЕЗ PYTHON (`mcp__workspace__bash`, `json.dumps(...).encode("utf-8")`), НЕ через PowerShell: PowerShell дважды кодирует вложенный KEYBOARD → лейбл кнопки становится кракозябрами (боевой прогон 17.07: «❌ Не согласен» → «â\x9dŒ Ð\x9dÐµ Ñ\x81Ð¾Ð³Ð»Ð°Ñ\x81ÐµÐ½»). Python кодирует правильно.
```python
POST(WEBHOOK + "imbot.message.add.json", {
  "BOT_ID": 23032, "CLIENT_ID": "ai_review_bot", "DIALOG_ID": "chat31598",
  "MESSAGE": "⚠️ #<lead_id> — <оператор>. <кратко суть проблемы>",
  "KEYBOARD": [{"TEXT":"❌ Не согласен","COMMAND":"disagree","COMMAND_PARAMS":"<lead_id>","BG_COLOR":"#E55D53","TEXT_COLOR":"#FFFFFF","DISPLAY":"LINE"}]
})
```
Дальше несогласие ведёт бот «Оценка AI» (кнопка → причина → proposal с кнопками Утвердить/Доработать/Отклонить → гейт одной роли). Текстовый `[FEEDBACK]` больше НЕ используется.

---

## Шаг 7 — Запись в CSV-лог

Дописать строки в `leads_log.csv` на Б24 Disk. Одна строка = один лид (ВСЕ лиды, не только проблемные):

```
дата,лид_id,ссылка,оператор,направление,классификация,флаг_C,флаг_E,тег_кц_AI,препарат_AI,статус_детальный_AI,задача_AI,согласие_с_оценкой
```

Поле `согласие_с_оценкой` при записи = пусто (несогласия ведёт бот «Оценка AI», см. Шаг 8).

---

## Шаг 8 — Обработка обратной связи: ведёт бот «Оценка AI» (23032)

С **2026-07-16** текстовый протокол `[FEEDBACK]` ВЫВЕДЕН ИЗ ЭКСПЛУАТАЦИИ (полная замена на бота). Несогласия ведёт отдельный бот «Оценка AI» (imbot 23032) на VM через кнопки:
- Менеджер жмёт «❌ Не согласен» под лидом → бот спрашивает причину (реплаем/тегом) → формулирует было→стало → постит proposal с кнопками ✅ Утвердить / ✏️ Доработать / ❌ Отклонить.
- **Гейт:** достаточно ОДНОГО голоса роли — Ласков `1`, Кирилл `8`, Старикова `11822`. Утверждённое → `applied.log`; отклонённое → `rejected.log` (на VM).
- **Применение в мастер-свод** `правила_анализа_лидов.md` делает scheduled-задача `lead-feedback-apply` (ежедневно 09:26, ПЕРЕД этим анализом). Только записи `chat31598`, идемпотентно, тест-чат chat36910 не применяется.

Поэтому lead-analysis на этом шаге НИЧЕГО текстового не обрабатывает и канон НЕ правит — этим занимается бот + scheduled apply. Полное описание: `РЕШЕНИЕ_lead-feedback_полное_описание.md`.

## Анализ по запросу

Если пользователь просит "разбор по операторам", "статистика за месяц" и т.п. — прочитать `leads_log.csv` с Б24 Disk и дать анализ по запрошенным срезам (оператор / направление / период / тип проблемы).

---

## Справочник правил (краткий)

Полные правила — в `references/field_rules.md`

| Код | Суть |
|---|---|
| R01 | UF-gap: звонок ≥15с, UF пуст → ⚠️ данные компактно (ID); не карточка; речь не судить; не mass launchCopilot |
| R02 | Рос. номер + активный запрос → ≥3 касания (≥1 TG/Max + ≥1 звонок) |
| R03 | Ссылка 1С заполнена → пациент записан |
| R04 | STATUS_ID только через crm.status.list, не хардкод |
| R05 | ФИО врачей сверять со справочником |
| R06 | Стадия "Запись" без ссылки 1С → ошибка администратора |
| R07 | Названия препаратов сверять с МНН (бралаксан=абраксан) |
| F01 | Упомянут препарат → поле Препарат НЕ "Не применимо" |
| F02 | ТегиКЦ: писать только если пусто и тег очевиден из разговора; сомнение → не писать или «Другие», не угадывать. Вариация приема (UF_CRM_1768816592560) — по field_rules отдельно |
| F03 | "Отказ пациента" только при явном отказе. Молчит → "Пациент не отвечает" |
